
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Profile pictures can be read by anyone
    // but can only be written by the authenticated user who owns them.
    match /profile-pictures/{userId} {
      allow read;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
